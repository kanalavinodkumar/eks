apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: eks-spot-cluster
  region: us-east-1

managedNodeGroups:
  - name: spot
    instanceType: t2.small
    spot: true
    desiredCapacity: 2
    ssh:
      publicKeyName: devops
